%% partest

problems = ['BNH()', 'SRN()','TNK()', 'Welded_Beam()'];
eim_methods = ['EIMnext_znorm','EIMnext'];
np = length(problems);
ne = length(eim_methods);


for i=1:np
    for j = 1:ne
    end
end